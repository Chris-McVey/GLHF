# Example overrides (Wikipedia-primary). Copy patterns into `nes.toml`.
#
# Membership = Wikipedia Western licensed + unlicensed retail (see CANONICAL_POLICY.md).
# Use this file only for enrichment fixes after `uv run curate.py`.

[year]
# Q12345 = 1990   # when RAWG returns the wrong regional year

[rawg]
# Q12345 = 54074  # pin cover art when fuzzy-match picks the wrong game

# [[supplement]]  # rare: only if Wikipedia omits a row you still want in the set
